const EllipticalCurve = ({ className }: { className?: string }) => {
  return (
    <div
      className={`${className} h-[500px] w-[1000px] rounded-[50%] border absolute`}
    />
  );
};

export default EllipticalCurve;
